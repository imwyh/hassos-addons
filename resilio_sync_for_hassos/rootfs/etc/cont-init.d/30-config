#!/usr/bin/with-contenv bash

# copy config
[[ ! -e /config/resilio_sync_for_hassos/sync.conf ]] && \
    cp -r /resilio_sync_for_hassos /config && mkdir /config/resilio_sync_for_hassos/storage

# permissions
# chown -R abc:abc \
#     /config
# chown abc:abc \
#     /sync
